bwf_declare_war_higher_protector_warning = {
	scope = character
	saved_scopes = {
		attacker
		defender
		target
	}

	is_shown = {
		bwf_border_war_higher_protector_will_join_trigger = yes
	}

	is_valid = {
		# Intentionally fails when a protector exists so BuildTooltip prints
		# the warning trigger's satisfied custom_description naming them.
		NOT = { bwf_border_war_higher_protector_warning_trigger = yes }
	}
}
